export const FILE_SIZE_LIMIT = 10 * 1024 * 1024;
export const ALLOWED_IMAGE_TYPES = ['image/jpeg', 'image/png', 'image/webp'];
export const DEFAULT_COLORING_OPTIONS = {
  blending: true,
  shadows: true,
  highlights: true,
  textures: false,
};
